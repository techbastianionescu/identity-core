services:
  api:
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      CORS_ORIGINS: https://identity.techbastian.com
  frontend:
    build:
      args:
        VITE_API_URL: /api
    ports:
      - "127.0.0.1:5173:80"
